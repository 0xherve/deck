# Implementation Plans

Generated by the improve skill on 2026-06-18. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Hono server + Commander CLI launches the app and serves the API | P1 | M | — | DONE |
| 002  | TanStack Router with app layout, sidebar, and route shells | P1 | M | 001 | DONE |
| 003  | Tiptap markdown editor with tabs, three view states, and auto-save | P1 | L | 002 | TODO |
| 004  | Git diff viewer with @pierre/diffs React component | P2 | M | 001, 002 | TODO |
| 005  | Read-only syntax-highlighted code viewer for non-markdown files | P2 | S | 003, 004 | TODO |
| 006  | Fix code review findings — error handling, Button reuse, async I/O | P1 | M | 001, 002 | TODO |
| 007  | Refine UI to Vercel/OpenAI design system aesthetic | P1 | M | 002, 006 | TODO |

## Dependency notes

- 002 requires 001 because the frontend needs the API server to fetch file trees and content.
- 003 requires 002 because it replaces the `/v/$` route placeholder and adds to the root layout.
- 004 requires 001 and 002 because it needs both the git-diff API endpoint and the `/d/$` route placeholder.
- 005 requires 003 because it modifies the non-markdown branch in the file view route created by 003. It also depends on 004 having validated that `@pierre/diffs/react` imports work.
- 006 requires 001 and 002 — fixes bugs and code quality issues found in code review.
- 007 requires 002 and 006 — design refinements build on the icon/Button fixes from 006.

## Recommended execution order

1. **006** first — fixes bugs, switches to Tabler icons, async I/O (prerequisite for 007)
2. **007** next — design refinements (prerequisite styling conventions for 003-005)
3. **003** — Tiptap editor (should follow 007's design conventions)
4. **004** — Diff viewer (can parallel with 003 in separate worktrees)
5. **005** — Code viewer (after 003 and 004)

All plans use `bun run typecheck` and `bun run lint` as verification gates.

## Findings considered and rejected

- `serveStatic` root path concern — `@hono/node-server` `serveStatic` uses `path.join(root, filename)`, so absolute paths work correctly. Not a bug.
- Path traversal bypass via prefix matching (e.g. `/app` matching `/app-evil`) — the guard uses `rootDir + path.sep` which prevents this. Not a bug.
